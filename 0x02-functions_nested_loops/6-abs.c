#include "main.h"
#include <stdio.h>

/**
 * _abs - Computes the absolute value of an integer
 *@r: number being tested
 * Return: Always 0.
 */
int _abs(int r);
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
